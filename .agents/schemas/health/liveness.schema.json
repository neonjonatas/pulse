{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://pulse.internal/schemas/health/liveness",
  "title": "Liveness Check Response",
  "description": "Response schema for the GET /health liveness probe. All NestJS services in the Pulse platform expose this endpoint. Returns a simple status indicator confirming the process is alive and accepting HTTP requests.",
  "version": "1.0.0",
  "owner": "platform",
  "domain": "platform",
  "createdOn": "2026-03-19",
  "updatedOn": "2026-03-19",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "const": "ok",
      "description": "Process liveness status. Always 'ok' if the endpoint responds."
    }
  },
  "required": ["status"],
  "additionalProperties": false,
  "examples": [{ "status": "ok" }]
}
